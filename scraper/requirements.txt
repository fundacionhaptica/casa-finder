# casa-finder — scraper deps
# Pinneamos minor; patch updates permitidos via ~=

# HTTP client (sync + async, retries, timeouts limpios)
httpx~=0.27.0

# HTML parsing
beautifulsoup4~=4.12.0
lxml~=5.3.0

# Modelos / validacion
pydantic~=2.9.0

# Fechas en formato espanol ("15 de julio de 2026")
python-dateutil~=2.9.0

# SQLite via stdlib — no requiere paquete
# Playwright — NO incluido (YAGNI). Si un portal lo necesita, se anade entonces.